header
    a(href="/").header__logo
        i.fab.fa-youtube

    nav
        ul 
            li
                a(href="/search")
                    i.fas.fa-search
            li 
                a(href="/") Home
            
            if loggedIn
                li 
                    a(href="/users/logout") Logout
                li 
                    a(href="/videos/upload") Upload Video
                li 
                    a(href=`/users/${loggedInUser._id}`) My Profile
                li 
                    a(href="/users/edit") Edit Profile
                
                li
                    a(href=`/users/${loggedInUser._id}`)
                        if loggedInUser.avatarUrl === undefined || loggedInUser.avatarUrl === ""
                            span 😀
                        else
                            if loggedInUser.avatarUrl
                                if loggedInUser.avatarUrl.includes("github")
                                    img.header__avatar(src=`http://localhost:4000/${loggedInUser.avatarUrl}`)
                                else
                                    img.header__avatar(src=`http://localhost:4000/${loggedInUser.avatarUrl}`)

            else 
                li 
                    a(href="/join") Join
                li 
                    a(href="/login") Login

                    GET https://localhost:4000/uploads/avatars/4e731ec9685b571b1c29c75810f75faf 